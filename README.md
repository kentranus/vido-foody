# Vido Foody POS App - Stable Build

Flutter + Node.js POS package.

This package keeps the POS interface stable and includes backend support for kiosk and online ordering.

Upload the contents of this folder to the POS GitHub repo root.
